/**
 * Auth layout — minimal centered card layout for login/register pages.
 * No sidebar, no header — just the auth form centered on screen.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
