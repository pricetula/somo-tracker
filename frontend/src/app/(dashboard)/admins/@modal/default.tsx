/**
 * Default fallback for the `@modal` parallel slot within the admins segment.
 * Renders nothing when the URL is `/admins` (no bulk-invite sub-route).
 */
export default function AdminsModalDefault() {
    return null;
}
