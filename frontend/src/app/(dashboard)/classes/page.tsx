/**
 * Classes Directory Page
 *
 * Lists all classes for the school with search, Linear-style filter
 * (by grades multi-select and active/inactive status), and a virtualized table.
 */

"use client";

import { ClassListPage } from "@/features/classes";

export default function ClassesPage() {
    return <ClassListPage />;
}
