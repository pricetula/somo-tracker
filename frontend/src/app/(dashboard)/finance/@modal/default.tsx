/**
 * Default fallback for the `@modal` parallel slot within the finance segment.
 * Renders nothing when the URL is `/finance` (no add sub-route).
 */
export default function FinanceModalDefault() {
    return null;
}
