/**
 * Default fallback for the `@modal` parallel slot within the nurses segment.
 * Renders nothing when the URL is `/nurses` (no bulk-invite sub-route).
 */
export default function NursesModalDefault() {
    return null;
}
