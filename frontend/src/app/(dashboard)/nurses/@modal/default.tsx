/**
 * Modal default — required by Next.js parallel routes.
 * When the @modal slot has no active intercepted match, this renders null.
 */
export default function NursesModalDefault() {
    return null;
}
