/**
 * Default fallback for the `@modal` parallel slot within the parents segment.
 * Renders nothing when the URL is `/parents` (no bulk-invite sub-route).
 */
export default function ParentsModalDefault() {
    return null;
}
