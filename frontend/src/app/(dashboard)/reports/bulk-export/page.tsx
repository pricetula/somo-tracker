/**
 * Bulk export page — admin bulk report generation.
 */

export default function BulkExportPage() {
    return (
        <div className="space-y-6">
            <h1 className="text-2xl font-bold">Bulk Export</h1>
            <p className="text-muted-foreground">
                Generate and export term reports in bulk for an entire class or grade level.
            </p>
            {/* TODO: Add class/grade selector and batch export UI */}
        </div>
    );
}
