/**
 * Streams page — manage school streams.
 *
 * Renders the same StreamsSection component used in /settings/school.
 */

import { StreamsSection } from "@/features/settings-school";

export default function StreamsPage() {
    return <StreamsSection />;
}
