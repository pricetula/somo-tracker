/**
 * Default fallback for the `@modal` parallel slot within the teachers segment.
 * Renders nothing when the URL is `/teachers` (no add sub-route).
 */
export default function TeachersModalDefault() {
    return null;
}
