/**
 * Timetable Structure page — Day Structural Matrix Portal.
 *
 * Renders the StructurePage container managing time-block partitioning
 * for school day templates.
 */

import { StructurePage } from "@/features/timetable-structure";

export default function TimetableStructurePage() {
    return <StructurePage />;
}
